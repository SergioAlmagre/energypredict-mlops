apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: energypredict-kv-secrets
spec:
  parameters:
    clientID: "REPLACE_ME_CLIENT_ID"
    keyvaultName: "REPLACE_ME_KEYVAULT_NAME"
    tenantId: "REPLACE_ME_TENANT_ID"
