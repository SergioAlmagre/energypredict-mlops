apiVersion: v1
kind: Service
metadata:
  name: energypredict-api
  labels:
    app: energypredict-api
spec:
  type: ClusterIP
  selector:
    app: energypredict-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
